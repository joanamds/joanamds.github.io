export interface ProjectTypes {
  title: string,
  descriptionENG: string,
  description: string,
  image: string | null,
  page: string | null,
  repo: string,
}

export interface ToolsTypes {
  icon: string,
  title: string,
}
